App(
    appid="desfire_seq_writer",
    name="DESFire Seq Writer",
    apptype=FlipperAppType.EXTERNAL,
    entry_point="desfire_seq_writer_app",
    requires=["gui", "nfc", "storage"],
    stack_size=8 * 1024,
    fap_category="NFC",
    fap_icon="icon.png",
    fap_version=(1, 0),
    fap_description="Write sequential CIDs to Akuvox-compatible DESFire EV3 cards",
    fap_author="Zankap",
    fap_weburl="",
)
